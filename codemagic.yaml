workflows:
  android-build:
    name: Android Build
    max_build_duration: 30
    environment:
      vars:
        PACKAGE_NAME: "com.rat.mobile"
    scripts:
      - name: Build APK
        script: |
          cd app
          gradle assembleDebug
    artifacts:
      - app/build/outputs/apk/**/*.apk